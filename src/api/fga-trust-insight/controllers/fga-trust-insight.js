'use strict';

/**
 * fga-trust-insight controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::fga-trust-insight.fga-trust-insight');
